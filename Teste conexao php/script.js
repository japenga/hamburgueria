function enviar(){
    this.window.location = "index2.html";
}
function voltar(){
    this.window.location = "index.html";
}

const elemento1 = document.getElementById("header");

function itens(){
    // elemento1.style.background = "rgba(255, 255, 255, 0.3)";
    // elemento1.style.zIndex = "1";
    import java.awt.Color;

    public class ExemploRGBA {
        public static void main(String[] args) {
            // Defina os valores de vermelho (R), verde (G) e azul (B) no intervalo de 0 a 255.
            int red = 255;
            int green = 0;
            int blue = 0;
    
            // Defina o valor alfa (transparência) no intervalo de 0 a 255, onde 0 é totalmente transparente e 255 é totalmente opaco.
            int alpha = 128; // Valor alfa para uma cor semi-transparente.
    
            // Crie uma cor RGBA usando os valores definidos.
            Color minhaCor = new Color(red, green, blue, alpha);
    
            // Você pode usar a cor em um contexto gráfico, como ao desenhar em uma tela ou painel.
            // Aqui, estamos apenas imprimindo os valores RGBA da cor criada.
            System.out.println("Valores RGBA da cor:");
            System.out.println("Red: " + minhaCor.getRed());
            System.out.println("Green: " + minhaCor.getGreen());
            System.out.println("Blue: " + minhaCor.getBlue());
            System.out.println("Alpha: " + minhaCor.getAlpha());
        }
    }
    
    
}
